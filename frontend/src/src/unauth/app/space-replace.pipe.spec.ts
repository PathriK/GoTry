import { SpaceReplacePipe } from './space-replace.pipe';

describe('SpaceReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
